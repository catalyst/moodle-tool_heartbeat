# .github/workflows/ci.yml
name: ci

on: [push, pull_request]

jobs:
  ci:
    uses: catalyst/catalyst-moodle-workflows/.github/workflows/ci.yml@main
    secrets: inherit
    with:
      # Any further options in this section
      disable_behat: true
      disable_grunt: true
      disable_phplint: true
      disable_phpdoc: true
      ignore_paths: errors
      enable_composer_publish: true
